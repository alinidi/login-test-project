export type CardProps = {
  source: string;
  title: string;
};

export type ButtonsProps = {
  firstButton: string;
  secondButton: string;
};

export type FormData = {
  login: string;
  password: string;
};
